# Contributing

Small, focused pull requests are preferred.

## Local Checks

Run the release-readiness stack before opening a pull request:

```sh
npm run release:check
```

## Scope

`clawplug` is an SDK for OpenClaw plugin definitions and tests. Keep changes typed, deterministic, and covered by local tests when they affect exported APIs.
